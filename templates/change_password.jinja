{% extends "base.jinja" %} {% block informacion %}
<div class="row bg-dark" style="display: flex; justify-content: center">
  <div class="bg-light text-center" style="width: 60%">
    <p>
      Hola <code id="correo">{{ correo }}</code> cambia tu contraseña
    </p>
    <form>
      <div class="mb-3">
        <label for="pwd1" class="form-label">Password</label>
        <input type="password" class="form-control" id="pwd1" />
      </div>
      <div class="mb-3">
        <label for="pwd2" class="form-label">Repita la password</label>
        <input type="password" class="form-control" id="pwd2" />
      </div>
      <button type="button" class="btn btn-primary" id="btn-enviar">
        Cambiar Password
      </button>
    </form>
  </div>
</div>
<script src="{{ url_for('static', filename='change_password.js') }}"></script>
{% endblock%} {%block titulo %} Cambiar Password 🔑 {% endblock %}
